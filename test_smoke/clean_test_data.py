'''
Explicit list of transient output paths under test_data/ that the
smoke suite may produce. hjlib-skeleton is pure vocabulary + index
helpers and does not emit files, so the list is empty.

Kept as a stub so the family convention (every repo has this file)
holds without exception.
'''

LIST_PATH_CLEAN: list[str] = []
